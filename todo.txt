1. rewrite usage docs in README for win/nix/osx platforms.
   or, maybe, create separate files for each OS.
   (usage-win.txt, usage-nix.txt, usage-osx.txt)

2. in rules, rename variables to avoid conflicts with other global variables:
   VERSION                  -> PKG_VERSION
   NAME                     -> PKG_NAME
   SRC_DIR_NAME             -> PKG_DIR_NAME
   TYPE                     -> PKG_TYPE
   URL                      -> PKG_URLS
   PRIORITY                 -> PKG_PRIORITY
   PATCHES                  -> PKG_PATCHES
   CONFIGURE_FLAGS          -> PKG_CONFIGURE_FLAGS
   MAKE_FLAGS               -> PKG_MAKE_FLAGS
   INSTALL_FLAGS            -> PKG_INSTALL_FLAGS
   EXECUTE_AFTER_UNCOMPRESS -> PKG_EXECUTE_AFTER_UNCOMPRESS
   EXECUTE_AFTER_PATCH      -> PKG_EXECUTE_AFTER_PATCH
   EXECUTE_AFTER_INSTALL    -> PKG_EXECUTE_AFTER_INSTALL

3. when Python is built, its '<prefix>/lib/python[x.y]' directory contains 'test' directory.
   it is necessary to decide whether it is needed in the python distribution archive.
   if no - in the 'scripts/cleanup.sh' script add the code in order to remove it.
